\documentclass[11pt]{article}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage[margin=1in]{geometry}

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newcommand{\NBC}{\operatorname{NBC}}
\newcommand{\Vol}{\operatorname{Vol}}
\newcommand{\calT}{\mathcal{T}}
\newcommand{\calO}{\mathcal{O}}
\newcommand{\1}{\mathbf{1}}

\title{The NBC Volume Identity and Fork-Packing Bound for Hard-Sphere Mayer Sums}
\author{ukiyois (research direction and maintainer)\\
AI-assisted Lean formalization and proof extraction}
\date{2026-08-27}

\begin{document}
\maketitle

\begin{abstract}
For a hard-sphere configuration, the connected Mayer graph sum is a signed
connected-subgraph count of its overlap graph.  A finite broken-circuit
cancellation turns that count into a fixed-sign number of no-broken-circuit
spanning trees.  Regrouping over the corresponding tree-owned regions gives
the exact positive volume identity.  In dimension three, an
attachment-aware packing of pairwise vertex-disjoint forks yields the stronger
per-tree estimate
$\Vol(\NBC_T)\le(17/32)^{\nu(T)}\kappa^{k-1}$.
The formal companion checks the pointwise NBC decomposition, the measurable
volume identity, the oriented fork factorization, and the explicit
measure-preserving coordinate transports.
\end{abstract}

This document is an explanatory mathematical translation of the formalization:
it records the proof architecture and the key intermediate identities in a
reader-friendly form.  The kernel-checked proof is the Lean development in
this repository; this TeX file is not itself a formal proof artifact.

\section{Definitions}

Fix $k\ge2$, dimension $d\ge1$, diameter $1$, and $r_1=0$.  For a
configuration $x=(r_2,\ldots,r_k)\in\mathbb{R}^{d(k-1)}$, define its overlap
graph
\[
 E(\calO(x))=\{\{i,j\}:|r_i-r_j|<1\}.
\]
For a graph $\Gamma$ on $[k]$, let $c_\Gamma(A)$ be the number of connected
components of $([k],A)$, and define
\[
 m(\Gamma)=\sum_{\substack{A\subseteq E(\Gamma)\\c_\Gamma(A)=1}}
              (-1)^{|A|}.
\]
Fix a strict edge order.  A broken circuit is a circuit with its largest edge
removed.  Write $\NBC(\Gamma)$ for the number of spanning trees containing no
broken circuit.

Let $\calT_k$ denote the finite set of spanning trees of the complete graph
$K_k$.  Equip the anchored configuration space with its product Lebesgue
measure; the notation $dx$ below refers to this canonical measure.

For completeness, there is an explicit bridge to the flat-coordinate model
used in much of the virial literature.  Write

\[
 X=\operatorname{Fin}(k-1)\to\operatorname{EuclideanSpace}(\mathbb{R},
     \operatorname{Fin}(d)),\qquad
 Y=\operatorname{EuclideanSpace}(\mathbb{R},\operatorname{Fin}((k-1)d)).
\]

The map $F:X\to Y$ first writes each Euclidean block in its real
coordinates, then sends $(i,j)$ to the corresponding coordinate of
\(\operatorname{Fin}((k-1)d)\) using the finite-index equivalence.
Thus $F$ is a bijective coordinate reordering.  The measure proof factors
into four explicit steps: the Euclidean coordinate representation in each
block, finite-product currying, reindexing by the finite-index equivalence,
and the inverse Euclidean coordinate representation.  Each step preserves
the corresponding product volume, so

\[
 F_*\,dx=dy.
\tag{0}
\]

In particular, for every measurable $A\subseteq X$,

\[
 \Vol_Y(F(A))=\Vol_X(A).
\tag{0a}
\]

The formal development defines the flat NBC region as $F(\NBC_T)$ and proves
(0a) for it.  This makes the usual $\mathbb{R}^{d(k-1)}$ presentation an
explicit transported version of the product-space statement, without
introducing a second unproved measure convention.

For a spanning tree $T$ of $K_k$, put
\[
 X_T=\{x:|r_i-r_j|<1\text{ for every }ij\in E(T)\},
\]
and
\[
 \NBC_T=\{x\in X_T:T\text{ contains no broken circuit of }\calO(x)\}.
\]
The definition of $X_T$ constrains tree edges only; other pairs may also be
active.

The connected Mayer weight and anchored cluster integral are
\[
 \omega_k(x)=\sum_{G\text{ connected on }[k]}
       \prod_{e\in E(G)}f_e(x),
 \qquad f_e(x)=-\1_{e\in E(\calO(x))},
\]
\[
 b_k=\frac{1}{k!}\int\omega_k(x)\,dx.
\]

\section{Restriction to the active graph}

\begin{lemma}
For every configuration $x$,
\[
 \omega_k(x)=m(\calO(x)).
\]
\end{lemma}

\begin{proof}
For a connected graph $G$, its product is zero unless
$E(G)\subseteq E(\calO(x))$.  In the latter case the product is
$(-1)^{|E(G)|}$.  Replacing $E(G)$ by $A$ gives exactly the sum defining
$m(\calO(x))$.
\end{proof}

\section{The finite broken-circuit cancellation}

\begin{lemma}[NBC cancellation]
For a connected graph $\Gamma$ on $[k]$,
\[
 m(\Gamma)=(-1)^{k-1}\NBC(\Gamma).
\tag{1}
\]
\end{lemma}

\begin{proof}
Call an edge $e$ a candidate for an edge set $A$ if there is a circuit $C$
whose largest edge is $e$ and such that $C\setminus\{e\}\subseteq A$.  Thus
$A$ contains a broken circuit exactly when it has a candidate.  For a bad
edge set $A$, choose the least candidate $e(A)$ and define
\[
 \tau(A)=A\mathbin{\triangle}\{e(A)\}.
\]
Let $C$ witness that $e(A)$ is a candidate.  The path
$C\setminus\{e(A)\}$ joins the endpoints of $e(A)$, so adding or removing
$e(A)$ does not change the connected components of $([k],A)$.  The same broken
circuit remains contained after the toggle, so $e(A)$ remains a candidate.

If $f<e(A)$, then $e(A)$ cannot belong to a broken circuit whose largest edge
is $f$, since every edge in that broken circuit is smaller than $f$.  Hence
the candidate status of every smaller edge is unchanged by the toggle.  It
follows that $e(\tau(A))=e(A)$ and $\tau(\tau(A))=A$.  The map $\tau$ is
therefore a fixed-point-free involution on the bad connected edge sets, and it
changes their cardinality by one.  Their signs cancel in pairs.

It remains to identify the fixed terms.  A connected edge set containing a
cycle contains the broken circuit obtained by deleting the largest edge of
that cycle.  Thus a connected edge set with no broken circuit is acyclic and
connected, hence is a spanning tree.  Conversely every NBC spanning tree is
not bad.  The only surviving terms in $m(\Gamma)$ are consequently the
$\NBC(\Gamma)$ trees, each with $k-1$ edges, proving (1).
\end{proof}

\begin{lemma}[Tutte specialization]
For connected $\Gamma$,
\[
 T_\Gamma(1,0)=\NBC(\Gamma),\qquad
 m(\Gamma)=(-1)^{k-1}T_\Gamma(1,0),
\]
where
\[
 T_\Gamma(u,v)=\sum_{A\subseteq E(\Gamma)}
 (u-1)^{c_\Gamma(A)-1}(v-1)^{|A|-k+c_\Gamma(A)}.
\]
\end{lemma}

\begin{proof}
At $u=1$, only $c_\Gamma(A)=1$ survives.  At $v=0$ the remaining term is
$(-1)^{|A|-k+1}$, which is $(-1)^{k-1}$ times the sign in $m(\Gamma)$.
Equation (1) then gives $T_\Gamma(1,0)=\NBC(\Gamma)$.
\end{proof}

\section{Pointwise NBC identity}

From the two preceding lemmas, for every configuration $x$,
\[
 \omega_k(x)=
 \begin{cases}
 (-1)^{k-1}\NBC(\calO(x)),&\calO(x)\text{ connected},\\
 0,&\calO(x)\text{ disconnected}.
 \end{cases}
\tag{2}
\]
Let
\[
 n(x)=\#\{T\in\calT_k:x\in\NBC_T\}.
\]
If $\calO(x)$ is connected, $x\in X_T$ is equivalent to
$E(T)\subseteq E(\calO(x))$, and the second condition in $\NBC_T$ is exactly
the NBC condition in $\calO(x)$.  If $\calO(x)$ is disconnected, neither side
counts a spanning tree.  Thus
\[
 |\omega_k(x)|=n(x)=\sum_{T\in\calT_k}\1_{\NBC_T}(x).
\tag{3}
\]

\section{NBC volume identity}

The analytic details needed to use (3) are part of the theorem and are included
explicitly.
Every edge activity is the indicator of an open norm inequality, so every
$X_T$ and $\NBC_T$ is Borel: there are only finitely many possible active
edge patterns, and the NBC condition is a finite Boolean formula in those
activities.  If $\calO(x)$ is connected, a path from $1$ to any vertex has at
most $k-1$ edges, each of length less than $1$.  Hence every coordinate of
$x$ has norm less than $k-1$.  If $\calO(x)$ is disconnected, $\omega_k(x)=0$.
Thus the integrand is bounded and supported in a finite-radius closed ball;
it is integrable, and every $\NBC_T$ has finite volume.

\begin{theorem}
For every $k\ge2$,
\[
 k!\,|b_k|=\int |\omega_k(x)|\,dx
 =\sum_{T\in\calT_k}\Vol(\NBC_T).
\tag{4}
\]
\end{theorem}

\begin{proof}
Equation (2) gives a constant-sign integrand, so
$|\int\omega_k|=\int|\omega_k|$.  Substitute (3).  The tree set
$\calT_k$ is finite, and all indicators are measurable and nonnegative, so
finite additivity of the integral gives
\[
 \int\sum_{T\in\calT_k}\1_{\NBC_T}(x)\,dx
 =\sum_{T\in\calT_k}\int\1_{\NBC_T}(x)\,dx
 =\sum_{T\in\calT_k}\Vol(\NBC_T).
\]
Finally use $b_k=k!^{-1}\int\omega_k$.
\end{proof}

\section{The three-dimensional fork-packing bound}

Now specialize to dimension $d=3$.  Let $\kappa$ be the volume of the unit
ball in the three-dimensional position space, and let $\nu(T)$ be the maximum
cardinality of a family of pairwise vertex-disjoint forks contained in the tree
$T$.  A fork has a center and two ordered neighbors.  For each selected fork,
the two tree-edge differences can be oriented so that they become

\[
 (r_b-r_a,\ r_c-r_a).
\]

On an NBC region, this pair belongs to the separated-pair region: both vectors
are in the unit ball and their mutual distance is at least one.  The exact
three-dimensional lens calculation gives

\[
 \Vol(\text{separated pair})=\frac{17}{32}\kappa^2.
\tag{5}
\]

Pairwise vertex-disjointness is the attachment condition that makes these
constraints simultaneous.  A packing of size $m$ uses $2m$ tree-difference
coordinates, leaving $k-1-2m$ coordinates.  The oriented coordinate map is a
measure-preserving rearrangement with sign changes on the selected coordinates,
so the remaining coordinates contribute unit-ball factors.  Consequently the
factorized volume is

\[
 \left(\frac{17}{32}\kappa^2\right)^m
   \kappa^{k-1-2m}
 =\left(\frac{17}{32}\right)^m\kappa^{k-1}.
\]

\begin{theorem}[Fork-packing upper bound]
For every spanning tree $T$ on $[k]$,
\[
 \Vol(\NBC_T)\le
 \left(\frac{17}{32}\right)^{\nu(T)}\kappa^{k-1}.
\tag{6}
\]
\end{theorem}

\begin{proof}
Choose a fork packing $P$ of maximum cardinality, so $|P|=\nu(T)$.  The
orientation-aware coordinate map sends the two selected differences for every
fork in $P$ to its relative pair and sends the complementary differences to
the remaining coordinates.  The map preserves product Lebesgue measure.  The
NBC region maps into the product of $|P|$ separated-pair regions and
$k-1-2|P|$ unit balls.  Apply (5) to the pair factors and multiply the
finite product of volumes.  This gives (6).
\end{proof}

\section{Relation to prior work}

Prior work on broken circuits, chromatic polynomials, and spanning trees gives
the discrete comparison framework.  Whitney's broken-circuit expansion,
Tutte's spanning-tree formulation, and Brylawski's broken-circuit-complex
treatment give the relevant graph background.  The connected-graph expansion
of the Mayer coefficient and the convergence results of Penrose and Ruelle
provide the statistical-mechanics comparison context.

The tree-graph literature is adjacent but not identical to the theorem proved
here.  Penrose's 1967 tree-graph identity and the minimum-spanning-tree
partition of Procacci--Yuhjtman rewrite or bound connected graph sums;
Lebowitz--Penrose derive virial convergence bounds from such estimates.
Ree--Hoover use a different modified-graph representation for hard-sphere
virial coefficients.  These are comparison sources only: no result from them
is imported as a Lean proof term, and this repository does not claim their
convergence or phase-transition conclusions.

\section{Contribution and distinction}

The formalized contribution has two linked parts.  Equation (4) converts the
configuration-dependent signed Mayer integral into a finite sum of nonnegative
tree-owned NBC volumes.  The fork-packing theorem then gives the quantitative
per-tree estimate (6), with one explicit factor $17/32$ for every available
vertex-disjoint fork.

The geometric result uses an attachment-aware construction rather than an
independent estimate on overlapping pairs.  The two differences attached to a
packed fork are oriented into a separated relative pair, while all complementary
differences remain in unit balls.  The complete rearrangement, including the
edge orientations, preserves product Lebesgue measure and is checked by the
Lean kernel.  This is the mechanism producing the compound-fork suppression.

Finite broken-circuit cancellation, the connected Mayer expansion, and the
product-to-flat coordinate map are supporting components of the checked
construction.  The formalized theorem combines them with the pointwise
treatment of the configuration-dependent overlap graph, the measurable
tree-owned decomposition, and the verified three-dimensional fork geometry.
The result is an exact identity together with an explicit geometric upper
bound, not a generic unsigned convergence majorant.

The recorded prior-work search did not identify an exact prior statement of
this continuous hard-sphere NBC-region volume identity or this fork-packing
specialization.  This is a bounded search result rather than an absolute
priority claim.  The comparison is therefore precise: prior finite NBC/Tutte
and Mayer/tree-graph results provide background, while the
submitted development supplies the configuration-dependent
pointwise-to-measurable-volume bridge and the quantitative fork-packing bound.
No claim of strict positivity, convergence, or phase-transition consequences is
made.

\begin{thebibliography}{99}
\bibitem{mayer1937} J. E. Mayer, ``The Statistical Mechanics of Condensing
Systems. I,'' \emph{Journal of Chemical Physics} 5(1) (1937), 67--73.
DOI: \texttt{10.1063/1.1749933}.

\bibitem{penrose1963} O. Penrose, ``Convergence of Fugacity Expansions for
Fluids and Lattice Gases,'' \emph{Journal of Mathematical Physics} 4(10)
(1963), 1312--1320. DOI: \texttt{10.1063/1.1703906}.

\bibitem{ruelle1963} D. Ruelle, ``Correlation Functions of Classical Gases,''
\emph{Annals of Physics} 25(1) (1963), 109--120.
DOI: \texttt{10.1016/0003-4916(63)90336-1}.

\bibitem{penrose1964} O. Penrose, ``Two Inequalities for Classical and Quantum
Systems of Particles with Hard Cores,'' \emph{Physics Letters} 11(3) (1964),
224--226. DOI: \texttt{10.1016/0031-9163(64)90418-4}.

\bibitem{lebowitzpenrose1964} J. L. Lebowitz and O. Penrose, ``Convergence of
Virial Expansions,'' \emph{Journal of Mathematical Physics} 5(7) (1964),
841--847. DOI: \texttt{10.1063/1.1704186}.

\bibitem{penrose1967} O. Penrose, ``Convergence of Fugacity Expansions for
Classical Systems,'' in A. Bak (ed.), \emph{Statistical Mechanics: Foundations
and Applications}, Benjamin (1967), p. 101.

\bibitem{whitney1932} H. Whitney, ``A Logical Expansion in Mathematics,''
\emph{Bulletin of the American Mathematical Society} 38(8) (1932), 572--579.
DOI: \texttt{10.1090/S0002-9904-1932-05460-X}.

\bibitem{tutte1954} W. T. Tutte, ``A Contribution to the Theory of Chromatic
Polynomials,'' \emph{Canadian Journal of Mathematics} 6 (1954), 80--91.
DOI: \texttt{10.4153/CJM-1954-010-9}.

\bibitem{crapo1969} H. H. Crapo, ``The Tutte Polynomial,''
\emph{Aequationes Mathematicae} 3 (1969), 211--229.
DOI: \texttt{10.1007/BF01817442}.

\bibitem{brylawski1977} T. Brylawski, ``The Broken-Circuit Complex,''
\emph{Transactions of the American Mathematical Society} 234(2) (1977),
417--433. DOI: \texttt{10.1090/S0002-9947-1977-0468931-6}.

\bibitem{reehoover1967} F. H. Ree and W. G. Hoover, ``Seventh Virial
Coefficients for Hard Spheres and Hard Disks,'' \emph{Journal of Chemical
Physics} 46(11) (1967), 4181--4197. DOI: \texttt{10.1063/1.1840521}.

\bibitem{procacciyuhjtman2017} A. Procacci and S. A. Yuhjtman, ``Convergence
of Mayer and Virial Expansions and the Penrose Tree-Graph Identity,''
\emph{Letters in Mathematical Physics} 107(1) (2017), 31--46; preprint
arXiv:1508.07379. DOI: \texttt{10.1007/s11005-016-0918-7}.
\end{thebibliography}

\section{Provenance and scope}

The research direction supplied by \texttt{ukiyois} was the key graph-theoretic
and NBC approach to the signed volume problem, together with the question
whether a positive decomposition might exist; \texttt{ukiyois} did not state
the exact identity proved here.  The upstream verified fact records are
identified by their internal worker authors as follows: fact
\texttt{2041d4f28d05949e} by \texttt{xhigh3}, fact
\texttt{4afee99f8b8e6010} by \texttt{xhigh5}, fact
\texttt{6d35ccd3e0232041} by \texttt{high3}, and fact
\texttt{7dcc52cfec5875b2} by \texttt{xhigh5}.  They are provenance records
only and are not imported as proof terms.  The Lean definitions and proofs were
implemented by AI agent sessions through OpenCode.  DeepSeek-V4-Flash was used
under its own model label.  At the time of the relevant runs, GLM-5.3-Flash had
not been publicly released; OpenCode exposed its free route under the alias
\texttt{ox-alpha}.  That alias refers only to the GLM route, not to DeepSeek.
The coordinate measure bridge and this final standalone extraction were added
in an OpenCode session using \texttt{openai/gpt-5.6-luna}.  No claim of strict
positivity of $b_k$ or of a separate complete sign-alternation theorem is made
here.

\section{Formal verification boundary}

The Lean companion does not import fact statements as proof terms and adds no
project-specific axiom.  The files
\texttt{NBCMatroid.lean}, \texttt{NBCGraph.lean}, and
\texttt{GraphicMatroid.lean} check the finite cancellation and graph
characterizations, while \texttt{MayerNBC.lean} checks the abstract Mayer
restriction, the pointwise NBC decomposition, and the finite-measure region
sum.  The dedicated file \texttt{HardSphereNBC.lean} specializes this chain
to
\[
  \texttt{HardSphereConfiguration k d}
    := \texttt{Fin (k - 1) -> HSPosition d},
\]
with particle $1$ at the origin and the product of Euclidean volume measures.
It proves the Borel measurability, bounded-support integrability, real-valued
absolute-value identity, and theorem (4) as
\texttt{hardSphere\_nbc\_volume\_identity}.  Fact IDs are provenance only;
they are not imported as proof terms.  The companion
\texttt{HardSphereMeasure.lean} proves (0), (0a), and the flat-coordinate form
\texttt{hardSphere\_nbc\_volume\_identity\_flat}.  The three-dimensional fork
modules prove the exact separated-pair volume, the maximum vertex-disjoint
fork-packing interface, and the orientation-aware measure-preserving
factorization used in (6).  The resulting Lean declarations are:
\begin{quote}
\texttt{hardSphereNBCRegion\_real\_volume\_le\_forkFactor}\\
\texttt{PalomarHS.nbc\_region\_real\_volume\_le\_fork\_factor}.
\end{quote}

\end{document}
